# Project Memory

以后处理这个项目时，先读这个文件。

## 项目信息
- 本项目是.net10实现的webssh服务
- 结构：`Program.cs`（Kestrel 后端，SSH.NET 管理 Linux）+ `client/`（Vue3 + Vite + Element Plus 前端，构建产物输出到 `wwwroot/`）
- 后端默认监听 **5101**（`PORT` 环境变量可覆盖；本机 shell 里 `PORT=0` 时会随机端口，属环境特性不是 bug）
- **运行方式（重要）**：用户直接运行 `bin/Debug/net10.0/HxServerFileManager.exe`，ContentRootPath=启动时 cwd，所以 **wwwroot 页面和 Data/connections.json 都从 exe 所在/启动目录读取**（bin 里的那一份）。改前端后必须 `npm run build` 且 **`dotnet build` 把 wwwroot 同步进 bin**，否则用户看到的还是旧页面。本地测试：`cd bin/Debug/net10.0 && PORT=5101 ./HxServerFileManager.exe`
- `test-linux/` 提供 Docker 测试机；真实测试机：`192.168.31.254:2222/2223`，`testuser/testpass`（用户自建）

## 长期规则
- 支持PC和手机端浏览（响应式布局，media query 单列切换）
- 每次对话即将结束时，如果有前端代码的修改一定要编译一次
## 重要文件
- `Program.cs`：全部后端逻辑（最小 API + 单文件类型定义）。**JSON body 绑定一律读 `ConnectionId`**（camelCase `connectionId`），前端 `api.js` 必须发 `connectionId` 字段，发 `connId` 会静默绑定为 null（历史教训，见已知问题）
- `client/src/App.vue`：多连接标签页架构。`connections[]` 存所有活跃会话，`activeId` 当前标签；标签可关闭（确认后断开）；工作区用 `v-show` 按连接渲染，保证每会话的终端历史/文件浏览器状态独立
- `client/src/api.js`：所有 /api 封装。POST 类接口统一发 `{ connectionId, ... }`；文件接口参数名与后端 record 一一对应（如 rename 用 `path`+`name`，不是 `dir`/`oldName`）
- `client/src/components/`：ConnectPanel（连接表单，可内联也可在对话框里复用）、SavedConnections、FileManager（el-table + 自绘面包屑）、Terminal（双模式：快捷命令 exec + 交互终端 xterm；**命令历史**：exec 回车 + 交互按回车记录到后端 Data/settings.json，弹窗双击再次执行）、LogPanel（SSE）、EditorModal
- `client/src/useSettings.js`：全局单例偏好（常用目录收藏 + 终端宏 + **命令历史**，同一份 Data/settings.json）；`addHistory`/`clearHistory` 本地先行更新再 POST/DELETE，失败静默（下次 GET 以服务端为准）
- 终端双模式：**快捷命令（exec）** 一次一命令带 cwd 持久化；**交互终端（interactive）** 用 SSH.NET `ShellStream`（pty，xterm-256color）+ 前端 `@xterm/xterm`，可跑 nano/vim/read 脚本等需要 TTY/stdin 的程序。输出走 SSE（`/api/terminal/stream`，先回放 ShellTail 再实时推送），输入走 `POST /api/terminal/input`，每会话惰性创建/关闭（`SshSession.EnsureShell/DisposeShell`），ShellOutput 为有界 Channel（DropWrite）防无消费者时积压，ShellTail 供新 SSE 消费者回放。切回 exec 模式会 DisposeShell（nano 等随之终止）
- 会话工作目录联动：`SshSession.Cwd` 为会话级 cwd（连接时初始化为 SFTP 工作目录）。`/api/command` 把命令包装为 `cd <cwd> && <cmd>; rc=$?; pwd; exit $rc`，解析末尾 pwd 行更新 cwd 并返回；`/api/cwd` 供文件列表导航时同步会话 cwd。前端 App.vue 持 `cwdMap`（每连接共享），FileManager「同步路径」checkbox（默认开）控制双向联动——开：终端 cd ⇄ 文件列表导航互相跟随；关：两边完全独立（终端 cd 不动文件列表，文件列表导航也不注入终端 cd、不同步 cwd）
- **交互终端路径同步（OSC 7）**：后端 `EnsureShell` 注入 `PROMPT_COMMAND`（printf OSC 7 序列 `\033]7;file://$HOSTNAME$PWD\007`）+ 自定义 PS1。bash 每次打印提示符前输出 OSC 7 携带当前目录；前端 Terminal.vue 在 SSE 数据里正则提取（跨 chunk 缓冲，`extractOsc7`），剥掉序列再 `xterm.write`，解析出的 path 通过 `update:cwd` 推给 App 同步文件列表。反向：App.vue 的 `onNavigate`（文件列表导航）调用 `termRefs[connId].injectCd(path)` 向交互终端写 `cd <path>\r`（Terminal 内判断是否交互模式；全屏程序运行时会被吞进程序，属预期）。**「同步路径」关闭时 `onNavigate`/`onCwdChanged` 直接 return，两端互不干扰**。OSC 7 依赖 bash（非 bash 时仅失去路径同步）
- `client/vite.config.js`：dev proxy 指向 `http://localhost:5101`
- `wwwroot`文件夹是client里的前端代码编译后的内容 不用再读取

## 登录鉴权（HxSimpleWebAuth）
- 鉴权库：`libs/HxSimpleWebAuth.dll`（从 BackDatabase 项目拷来，net8 程序集 net10 可引用；csproj `<Reference HintPath=libs\...>` + `<None Include=libs\**\* CopyToOutputDirectory>`，构建会拷到 bin）。API：`new WebAdminAuth(password, logDirectory, envVarName)`；`Authorize(HttpRequestData)` 校验 Bearer token；`Handle(HttpRequestData, path)` 处理登录/登出（登录 POST body 用 `{"key": "<密码>"}`，返回 `{token, expiresAt}`，错误时 `{error, locked, remainingAttempts}`）；`IsAuthPath(path)` 判定 `/api/auth/login|logout`；`HttpRequestData(Method, Target, Headers, Body, RemoteIp)`；`ApiResponse(StatusCode, Body, AllowHeader)` + `Json/Error` 静态方法。**token 只认 Authorization: Bearer 头，不认查询参数**
- 密码配置：**优先级 ① 环境变量 `HXSFM_WEB_PASSWORD`（可覆盖）→ ② `configs/env.json` 的 `authPwd` 字段（模板 `configs/env.json.example`，`LoadConfigPassword()` 读取）**。`configs/env.json` 已加入根 .gitignore（存密码不入库），`env.json.example` 可提交。**配置了密码**：所有 /api（除 /api/session 与 /api/auth/*）必须带有效 token；**未配置**：仅本机回环可访问（fail-closed，防止内网裸奔；手机/局域网访问必须设密码）
- 上传上限：**优先级 ① 环境变量 `HXSFM_MAX_UPLOAD_MB`（可覆盖，0=不限制）→ ② `configs/env.json` 的 `maxUploadMb` 字段（模板 `configs/env.json.example`，`LoadConfigMaxUploadMb()` 读取）→ 默认 1024MB（1GB）**；0/负数 = 不限制（Kestrel 不设 MaxRequestBodySize、`/api/health` 返回 `maxUploadBytes=0`，前端跳过预校验）。**桌面壳（HxServerFileManager.Desktop）启动时强制设 `HXSFM_MAX_UPLOAD_MB=0`，忽略大小限制**（本地窗口直连 localhost）
- 中间件位于 `app.Use(...)`（静态文件之前），`CreateAuthRequest(Async)` 把 HttpContext 转 HttpRequestData；**EventSource/<a download> 带不了请求头**，前端把 token 放 `?token=` 查询参数，中间件检测到无 Authorization 头时自动补 `Authorization: Bearer <token>` 再校验（实测 SSE/download 均通过）
- 端点：`GET /api/session`（返回 `{required, authenticated}`，前端据此显示登录页）、`POST /api/auth/login`、`POST /api/auth/logout`（吊销 token，之后原 token 变 401）
- 前端（api.js）：token 存 sessionStorage + 勾选记住时存 localStorage（`hxsfm_auth_token`）；`request()` 统一带 Bearer 头，401（非登录接口）触发 `setUnauthorizedHandler` → App 清状态回登录页；`api.login` 单独处理 401 拿 locked/remainingAttempts 展示；下载 URL 与 SSE URL 追加 `?token=`。App.vue：`checkSession` 探测后决定渲染 LoginView 还是主界面，**认证通过后才 loadSaved + restoreWorkspace**；退出登录会吊销 token + 逐个断开 SSH 会话 + 清 localStorage；登录成功（onAuthed）后同样恢复会话/路径

## 已知问题
- **Desktop 桌面壳白屏的坑（勿回退）**：① Photino.NET 必须 ≥ **4.0.16**（3.0.14 在 Windows 上建出的窗口坏掉，只剩标题栏大小）；② Photino 窗口必须在 **STA 线程** 创建——.NET 主线程默认 MTA，WebView2 在 MTA 线程初始化直接报 0x80010106（RPC_E_CHANGED_MODE）且被 Photino 静默吞掉，表现就是窗口全白、进程下没有任何 msedgewebview2 子进程；③ 入口保持同步（top-level 不能有 await）。排查手法：看进程有无 webview2 子进程 + Kestrel 日志有没有 WebView2 发出的 `GET /`（之前两次"修复"后实际没有，窗口根本没导航）
- 曾批量出现“字段名不匹配”：前端发 `connId`，后端绑 `ConnectionId`，导致 disconnect/reconnect/mkdir/rename/delete/command/save 全部静默失败或 404。已全部统一为 `connectionId`。**新增后端接口时务必核对 body 字段名**
- 后端最小 API 返回体一律 camelCase（`FileEntry` 序列化为 `name/fullPath/isDirectory/size/lastWriteTimeUtc/isText`），前端 FileManager 曾误读 PascalCase 导致列表空白，已改 camelCase。**前端读响应字段时一律用小写开头**
- 后端会话空闲 30 分钟自动回收（ConnectionManager.CleanupLoop）；前端标签不会自动消失，此时操作会报“连接不存在或已断开”，需手动断开重连
- SSE 端点（`/api/logs/stream`、`/api/terminal/stream`）在客户端断开后**不要返回 `Results.Ok()`**——响应已开始会抛 "StatusCode cannot be set because the response has already started"，用 `Results.Empty`（不触碰状态码）
- Docker 端到端（连 test-linux）尚未跑：开发环境无 docker
- `connections.json` 明文存密码/私钥，仅限本地/内网

## 进度记录
- 2026-08-17：server 启动脚本随发布带入 + 后台运行 —— ① **`build.sh server` 发布 Linux 服务端时自动把 `start-linux.sh` 拷进产物目录并 chmod +x**（此前产物目录里没有启动脚本，用户得手动找仓库里的）；`build_server()` 里同时对主程序二进制 `chmod +x`（Windows 构建机产出的 ELF 默认无执行位，实测 -rw-r--r--，直接部署到 Linux 会“未找到”）。⚠ 注意：Windows Git Bash 下 `chmod +x` 对新建文件可能不生效（MSYS 权限位不落盘），**真正的兜底在脚本侧**：`find_binary` 用 `-f` 匹配后用 `[[ -x ]] || chmod +x` 补执行位再启动（实测：Windows 打 tar 传到 Linux 丢执行位，脚本首跑自动补上）。② **`start-linux.sh` 重写支持前后台/管理动作**：默认**后台启动**（`nohup ... >>hxsfm.log 2>&1 &` + PID 写 `hxsfm.pid`，1 秒后探测存活，启动失败打印日志尾部并退出码 1）；`-f/--foreground` 前台（Ctrl+C 优雅停机）；`stop`（SIGTERM 等最多 6s，超时强杀，清 PID 文件）；`restart`；`status`；`-h` 帮助。支持位置参数端口、`.env` 加载、`PORT` 等环境变量透传。实测（真机 Alpine）：后台启动→status→stop→restart→`-f` 全流程通过；Alpine 缺 glibc/libstdc++ 时二进制跑不起来属环境限制（可用 `-r linux-musl-x64` 发布 musl 版，但容器仍需 `apk add libstdc++ libgcc`），脚本的失败检测路径在该场景下工作正常（1s 内识别 + 清 PID + 打印日志尾部）
- 2026-08-17：连接列表「重连」→「连接」+ 立即开 tab —— 右侧「已保存的连接」列表的按钮原来叫「重连」，且 `doReconnect` 等连接成功后才 emit 开 tab；改为与顶栏「已保存连接」下拉一致：按钮改「连接」，点击 emit('open', item) → App.vue `@open="openSaved"` 立即开「正在连接…」占位 tab 再后台重连（`openSaved` 同一套流程）。SavedConnections 里 `busyId`/`doReconnect` 已删（占位 tab 本身就是进度提示）。
- 2026-08-17：删除非空文件夹修复 —— `/api/delete` 对目录用 SFTP `DeleteDirectory`，该调用只删空目录，目录里有文件直接抛异常（前端表现为“删除失败：No such file / 无法删除”）。且实测（busybox sftp-server）`sftp.DeleteFile` 对符号链接会**跟随并删除链接目标**（删 link → 目标文件一起没，危险）；`GetAttributes` 对文件链接也返回目标属性，导致 IsSymbolicLink 检测不可靠。修复：**删除一律走远端 `rm -rf -- <Shq(path)>`**（文件/空目录/非空目录/符号链接通吃；链接只删链接本身不追进目标；`--` 防名称以 `-` 开头；exit≠0 时把 stderr 返回给前端）。安全守卫：name 含 `..` 或目标为 `/` 时 400 拒绝。实测（真机）：嵌套目录+文件+链接+`-` 开头目录全删干净、链接目标幸存、单文件链接只删链接、空目录/带空格名正常、根目录/穿越被拦。
- 2026-08-17：命令历史 + 版本号 —— ① **历史 SSH 命令查看/双击执行**：Terminal 实际执行过的命令记入 `Data/settings.json`（`UserSettings` 加 `History`，`CommandHistoryItem`：connKey/command/cwd/exitStatus/createdAt；旧 settings.json 无 History 字段时 `Load()` 归一化为空列表，勿忘）。记录来源：快捷命令模式 `run()` 完成后（带 exitStatus 与执行时 cwd）；交互终端模式 `sendInput` 按回车切分输入缓冲（控制字符/`\x1b` 转义序列/方向键清缓冲防拼出脏行，Ctrl+C 清缓冲）。端点 `GET/POST/DELETE /api/settings/history`（POST 同连接同命令去重置顶、每连接上限 200 条；DELETE 带 connKey 只清该连接、空清全部）。前端 `useSettings.history` 全局状态 + `addHistory`/`clearHistory`（本地先行 + 后端持久化，失败静默）；Terminal 宏条加「命令历史」按钮 → 弹窗表格（时间/命令/目录/状态，双击一行或点「执行」再次执行：交互模式 `sendInput(cmd+'\r')` 并清残留缓冲，快捷命令模式填入输入框直接 run；「清空本连接历史」确认后 DELETE）。实测（真机 192.168.31.254）：exec 执行、交互执行、双击重跑、按连接隔离、去重、清空全部通过。② **版本号**：两项目 csproj 加 `<Version>1.0.0</Version>`；`WebHost.AppVersion()` 读 `AssemblyInformationalVersion`（去 `+哈希` 后缀）——HX 启动打印 `版本 1.0.0`、`/api/health` 返回 `version` 字段；桌面壳窗口标题 `HxServerFileManager v1.0.0`（读主项目程序集，与 HX 一致）。改版本号时两个 csproj 一起改。
- 2026-08-16：网络状态永远为空的根因 + 实时上下行 —— ① **根因：CRLF 泄漏进远端 shell**。`SystemStatusHelpers.Script` 是本文件里的 raw string，Program.cs 是 CRLF，脚本逐字发给远端后每行尾都带 `\r`；NET 段的 awk 命令最后一行以 `2>&1` 结尾，远端 bash 读成 `2>&1\r`（`&1\r` 不是合法 fd，被当文件名）→ **`ambiguous redirect`，整条 awk 不执行**，所以 nets 永远是 `[]`（其它段的 `2>&1` 后面还跟着 `|| echo ...`，`\r` 落在行尾不影响，因此只有网络挂）。诊断：给 `/api/system-status` 临时加 `raw=1` 直接看 `cmd.Result`+`cmd.Error`，stderr 里就是 `bash: line 21: 1^M: ambiguous redirect`（真实测试机 192.168.31.254:2222 Alpine/busybox 复现）。修复：`Script = Sh(ScriptRaw)`，`Sh()` 把 CRLF 换成 LF；**以后凡是把源码里的多行脚本发给远端，一律先过 `SystemStatusHelpers.Sh()`**。② **实时上下行（MobaXterm 风格）**：新增 SSE `GET /api/net-stream?connId=&interval=1`（1-10s，默认 1s）——一条**常驻 exec 通道**里 `while :; do cat /proc/net/dev; echo ===TICK===; sleep N; done`（不用远端 awk、不读 `/sys`，最小化容器也能跑；`cat` 遇到管道关闭 `|| exit 1` 让远端循环随客户端断开自然终止，实测断开后远端无残留 sleep/cat），后端 `ParseNetDev`（rx=冒号后第 1 列、tx=第 9 列，跳过 lo 与两行表头）+ `WithRates` 按相邻两帧算 B/s，每帧 `s.Touch()` 防空闲回收，`shutdownCts` 已链入。**PipeStream 的 Read 阻塞且不理取消令牌**，所以后台线程逐行读进有界 Channel，SSE 循环 `ReadAllAsync(cts.Token)` 消费；退出时 `using cmd` 释放通道让阻塞的 Read 返回。③ 前端：`openNetStream()`（EventSource，token 走 `?token=`），SystemStatus 迷你条网络区显示 `↓/↑` 实时速率（首帧 warmup 显示「采样中…」），详情表 `viewNets` = 轮询的 state + 实时流的速率/累计字节（轮询没采到网卡时用实时流兜底）；删掉前端自己按 10s 轮询算差值的逻辑（后端 `ApplyRates` 保留作流未连上时的兜底）。实测：空闲 ~0.7KB/s，远端 40MB base64 经 SSH 下发时 tx 峰值 ~21.9MB/s。
- 2026-08-16：磁盘视图排除 docker/containerd 容器挂载 —— 磁盘空间查看（SystemStatus 迷你条 + 详情弹窗表格）会列出一大堆 `/var/lib/docker/overlay2/<hash>`、`/var/lib/docker/containers/<id>/shm`、containerd snapshot 挂载（每容器一条、同一磁盘重复数据）。修复：**后端** `SystemStatusHelpers.Script` 的 df awk 加 `$6 !~ /^\/var\/lib\/docker\//` 和 `$6 !~ /^\/var\/lib\/containerd\//`（busybox awk 实测通过；`/var/lib/docker` 根挂载本身保留，能看到数据盘真实占用）；**前端** SystemStatus.vue 加 `isContainerMount()`，详情表格改绑 `viewDisks`、迷你条 `mainDisks` 也过滤（双保险，防旧后端）。
- 2026-08-16：批量下载报错修复 + 停止下载 —— ① **批量下载/下载中途连接被掐断（“Error while copying content to a stream / response ended prematurely”）根因**：流式端点（`/api/download`、`/api/download-many`）直接 `ctx.Response.Body.WriteAsync` 后**返回 `Results.Ok()`**，响应已开始后再 set_StatusCode 抛 `InvalidOperationException: StatusCode cannot be set because the response has already started`，Kestrel 把已发送一半的连接直接掐断（服务端日志可见该异常；客户端 HttpClient 报 `HttpRequestException: Error while copying content to a stream.` ← inner `HttpIOException: The response ended prematurely`）。修复：两处改 `Results.Empty`（与 SSE 端点同一坑，勿再回退）。实测：多选目录+文件、300 文件、40MB tar 全部完整解包，服务端日志 0 异常。② **批量下载「停止下载」**：前端进度面板加停止按钮；`desktopDownloadMany` 改返回 `{promise, cancel}`，发消息带 `id`，新增 `downloadManyCancel` op → C# `ActiveDownloads` 注册表按 id 取消 `CancellationTokenSource`（与 `AppState.ShutdownToken` 链接）→ HttpClient 请求中止 → 服务端 `RequestAborted` → 远端 tar 终止 → 本地 `CleanupPartial` 倒序删除本次已解包的文件/空目录（不动用户原有内容）→ 回 `{canceled:true}`，前端提示「已取消批量下载」。浏览器端批量（逐个 `<a download>`）也支持取消（终止循环）。③ 顺带：桌面端 `PostAsync` 默认 `ResponseContentRead` 会把整个 tar 缓冲进内存，改 `SendAsync(ResponseHeadersRead)` 流式；tar 命令加 `--` 结束选项解析（选中项以 `-` 开头时 busybox tar 会当选项解析导致整包 0 输出，实测 busybox 1.37/GNU 都支持 `--`）；错误回执带 `innerError`（外层包装消息隐藏了真实根因）。
- 2026-08-16：批量下载（多选文件/文件夹 → 选一个本地文件夹整包下载）—— 「操作」下拉新增「下载」，多选或含目录时：**桌面端**弹 Win32 文件夹选择器（`SHBrowseForFolder` + BROWSEINFO，`BIF_RETURNONLYFSDIRS|BIF_NEWDIALOGSTYLE`，实测对话框正常弹出；注意 NEWDIALOGSTYLE 下 lpszTitle 显示在对话框内部、标题栏仍是「浏览文件夹」）选一个目录 → 远端 `tar -C <parent> -cf - <names>` 打包流式下发 → C# 用 `System.Formats.Tar.TarReader` 逐条目解包到所选目录（**保留目录结构**，逐条目回传 `downloadManyProgress` 进度节流 80ms，完成后 `downloadManyResult` 带文件数）；单个文件保持原「另存为」对话框行为（可改文件名）。**浏览器端**无选文件夹能力：逐个 `<a download>` 下载文件（间隔 300ms 防拦截），选中的目录跳过并提示。
  - 后端新增 `POST /api/download-many`（body `{paths}`，connId 走查询串，token 同其它下载）：校验路径绝对 + 同目录 + `Sftp.Exists` 预校验 → **`SshCommand.BeginExecute()` + 读 `OutputStream` 流式消费**（`Execute()` 会把全部输出缓冲进内存，大目录扛不住；`EndExecute` 在流读完后调用，中途异常则靠 using 关通道终止远端 tar）→ 64KB 块写响应 + 每块 `s.Touch()` 防空闲回收。**坑（实测踩到）**：Docker 等环境的 **busybox tar 不支持 `--ignore-failed-read`**（直接失败 0 输出），必须去掉该选项，存在性靠预校验兜底。busybox tar 产 ustar 格式，`System.Formats.Tar` 可正常读；中文文件名（UTF-8）正常。
  - 前端：`api.downloadManyUrl(connId)` + `desktopDownloadMany(url, paths, onProgress)`（Promise 回 path+count）；FileManager 操作菜单「下载（N）」+ 批量下载进度面板（复用 upload-panel 样式，不定长进度条 + 当前文件名 + 已解 N）；下载中禁用菜单、`tableLoading` 纳入 downloading。**右键菜单也要尊重多选**（用户实测发现右键下载仍走单文件）：`onRowContextMenu` 右键未选中行时先把它设为选中（clearSelection + toggle，标准文件管理器行为），`menuDownload`/`menuDelete` 在右键项属于多选集合时走批量（`downloadSelected`/`batchDelete`），否则单文件。另修：C# 处理器外层 catch 按实际 op 回对应 Result（原来一律回 saveFileResult，downloadMany/downloadFile 出错时前端一次性回调永远收不到而挂起）。验证：真实测试机多选（目录 test/logs + 文件 1.html）→ 35MB tar 结构完整；TarReader 解包探针落地 10 文件含中文名；文件夹选择器探针 DIALOG_FOUND=True；预览注入模拟 Photino 桥（bridge-test 页面：先定义 window.external 再加载 app，api.js 初始化时才会注册 receiveMessage）驱动多选→右键下载→`{op:'downloadMany', url, paths}` 消息正确发出、模拟回执后提示「已下载 2 个文件到 …」
- 2026-08-16：桌面下载/导出修复 —— ① **默认文件名**：改用 Win32 `GetSaveFileName`（`Win32Dialogs.SaveFile`：comdlg32 P/Invoke + OPENFILENAME 结构体）替代 Photino `ShowSaveFile`——彻底绕开 Photino 4.0.22 native Create() 的 HRESULT 覆盖 bug（见下一条），且支持**预填默认文件名**（下载 = 远端文件名；导出 = hxsfm-connections-日期.json）。**坑**：含 StringBuilder 字段的结构体 `Marshal.SizeOf` 报“无法计算大小”，改用定长填充串 `(defaultName??"").PadRight(1024,'\0')` + `nMaxFile=1024`（marshaler 按串长分配 1025 字符缓冲区、预填文件名框），回读按第一个 `\0` 截断；64 位下 SIZE=152（OPENFILENAMEW，实测对话框正常弹出）。② **下载超时/闪退**：a) 服务端 `/api/download` 从 `Results.File` 改**手动流式**（64KB 块循环 `ReadAsync`/`WriteAsync` + 每块 `s.Touch()` 防 30 分钟空闲回收断掉下载中的会话；`Results.File` 的流由 Kestrel 托管，中途出错只能默默断连），并关 Kestrel `MinResponseDataRate`（默认 240B/s+5s 宽限且对响应生效，慢速/大文件下载会被掐断成“超时”）；b) 桌面壳 `HttpClient.Timeout = Timeout.InfiniteTimeSpan`（不再 2 小时）；c) **关闭守卫防闪退**：窗口关闭 handler 调 `AppState.BeginShutdown()`（置 `WindowClosing` + 取消 `ShutdownToken`）→ 在途下载经 token 取消（`GetAsync`/`CopyToAsync` 传 `AppState.ShutdownToken`），`DesktopBridge.SendResponse` 先判 `WindowClosing` 再 `SendWebMessage` 且 try/catch 吞异常——此前窗口关闭/请求中断瞬间对已销毁窗口 SendWebMessage 会原生崩溃闪退。③ 下载对话框 `lpstrDefExt` 按远端扩展名设置（用户删掉扩展名时 Windows 自动补回），保留手动补扩展名兜底
- 2026-08-16：桌面壳导出选保存路径 —— 导出连接在桌面端弹原生「另存为」对话框选路径，浏览器端保持 `<a download>` 默认下载。① 后端 `/api/health` 增加 `desktop` 字段（读 `HXSFM_DESKTOP` 环境变量）。② 桌面壳（Desktop/Program.cs）：启动时设 `HXSFM_DESKTOP=1`；Photino 窗口注册 `RegisterWebMessageReceivedHandler` 处理 `{op:'saveFile', defaultName, content}` → `ShowSaveFile(title, defaultPath, (string,string[])[])` 原生保存对话框（过滤串 native 端按 `名字|模式` 拆，扩展名自动加 `*.` 前缀、`*` → `*.*`）→ 确认后 `File.WriteAllText` 写文件 → `SendWebMessage` 回 `{op:'saveFileResult', ok, path|canceled|error}`；`DesktopBridge` 静态类（JsonSerializerOptions camelCase）+ `DesktopRequest` record，类型声明必须在所有顶级语句（含局部函数）之后。**⚠ Photino 4.0.22 坑（实测踩到）**：`defaultPath` 必须传 **null**——native `Create()` 用 `SHCreateItemFromParsingName` 的返回值**覆盖** `CoCreateInstance` 的成功 HRESULT，而保存目标文件尚不存在、解析必失败（0x80070002）→ 回调里传文件名时 `ShowSaveFile` 10ms 返回 null、**对话框不弹**；传 null 跳过该代码块才正常弹出（独立 Photino 测试程序 EnumWindows 实测验证）。代价：无法预填文件名，用户在对话框里自己输入；保存前对路径做 `.json` 后缀校验（`EndsWith(OrdinalIgnoreCase)`，没有则补 `.json`）。④ 桌面端**文件下载**同样走原生「另存为」：前端 `desktopDownloadFile(url, defaultName)`（op=`downloadFile`，不设超时）+ FileManager `download()` 按 `isDesktop()` 分流；C# 弹保存对话框后**后台 `Task.Run` 流式下载**（HttpClient GET 前端传的绝对 URL，`HttpCompletionOption.ResponseHeadersRead` + 流式 CopyToAsync 落盘，大文件不阻塞 UI 也不走 JS 桥传内容；`client.Timeout` 2 小时防慢速连接被掐断），用户删掉扩展名时按原文件名补回；完成后回传 `downloadFileResult`。③ 前端 api.js：桌面消息桥（仅 Photino WebView2 存在）+ `isDesktop()`（缓存 health.desktop）+ `desktopSaveTextFile(defaultName, content)`（Promise：路径/取消 null/超时 60s 拒绝）；SavedConnections.vue `doExport` 按 `await isDesktop()` 分流。**坑（实测踩到）**：Photino 注入的 `window.external.receiveMessage` 是**注册回调的函数**（`receiveMessage(cb)`，内部监听 chrome.webview message 事件），不能赋值覆盖，否则 C#→JS 响应收不到导致「保存对话框无响应」超时；且**构建时必须先停掉在跑的 exe**（锁文件会让 dotnet build 复制 exe 失败、连带 wwwroot 同步 target 不执行，bin 里一直是旧前端）。**（⚠ 此条已被下一条取代：对话框已改用 Win32 GetSaveFileName，不再受 defaultPath bug 限制，且能预填默认文件名）**
- 2026-08-16：交互终端剪贴板 —— ① **选中即复制**：xterm `onSelectionChange` 监听选区变化，非空即写入剪贴板（优先 `navigator.clipboard.writeText`，页面聚焦时免授权；失败退回隐藏 textarea + `document.execCommand('copy')`，兜底路径结束后 `xterm.focus()` 还焦点）。② **右键粘贴**：XTerm 选项 `rightClickSelectsWord: false`（右键不再选单词），在 `termHost` 上接管 `contextmenu` 事件（preventDefault + stopPropagation）→ `navigator.clipboard.readText()` 读剪贴板后发送；WebView2 的 ClipboardRead 权限只拦「无用户手势」读取，右键是用户手势可直接读；读取失败提示用 Ctrl+V。③ **回车执行确认**：粘贴内容以 `\r`/`\n` 结尾时弹自定义 el-dialog（textarea 可编辑）——「执行」发送（可能编辑过的）内容并补回车运行、「仅粘贴」去掉末尾换行只发送不执行、「取消」放弃；Ctrl+Enter 快捷执行，关窗后 `xterm.focus()` 恢复终端焦点。不改后端
- 2026-08-16：服务器间直传（发送到连接，不经本机中转）—— 在两个已打开的连接之间直接传文件/文件夹：数据全程在两端服务器之间流动（源机执行 scp 直连目标机），本机只下发指令 + 轮询状态。① 后端 `POST /api/server-copy`（源/目标连接 id + 选中项绝对路径 + 目标目录）：校验两会话存在、源≠目标（host|port|username 相同拒绝）、路径绝对；目标目录不存在时用**目标机已有 SFTP 会话** `EnsureRemoteDir` 补建；`SshSession` 新增连接元数据（Host/Port/Username/Password/PrivateKey/Passphrase，ConnectInternal 填充）。② 作业模型 `ServerCopyJobs`：内存注册表 + 后台 `Task.Run`，逐项在**源机**执行 `scp -r -P <port> -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ConnectTimeout=15`；目标认证：密码 → 源机有 sshpass 则 `sshpass -p` 喂密码，没有则先试免密（失败提示装 sshpass 或配密钥互信）；私钥 → base64 经 shell 写 /tmp 临时文件 + `scp -i`，结束即删（带口令私钥不支持，明确报错）；路径安全：源路径 `Shq`、远端路径 `EscapeScpRemote` 反斜杠转义后整体 Shq（scp 把 : 后路径交远端 shell 解析，两层转义防空格/特殊字符）；`GET /api/server-copy/{jobId}` 轮询每项 pending/running/done/failed + 总体 state；作业完成 1 小时后清理；`cmd.CommandTimeout` 2 小时防挂死。③ 前端：FileManager「操作」下拉加「发送到连接…」（无选中项或没有其他连接时禁用，带选中数量徽标）；App 弹对话框：单选目标连接（排除当前 tab + 按 host:port:username 去重，pending 占位 tab 不算）、目标目录输入（默认 = 源选中项所在目录，回车即发）；发送后 1.2s 轮询进度（el-progress + 每项状态图标/失败原因），完成时把目标连接的 FileManager `refreshToken` +1 触发其列表刷新
- 2026-08-16：上传文件夹 + 拖拽上传 —— ① 文件列表支持**拖拽上传**（浏览器文件选择框无法同时选文件+文件夹，拖拽是唯一能混选的方式）：面板 `@dragenter/@dragover/@dragleave/@drop`，拖入时浮出「松开以上传文件 / 文件夹」遮罩（`.drop-overlay` z-index 4000，高于 upload-panel 的 3000）；`dataTransfer.items → webkitGetAsEntry()` 递归遍历（`collectEntries`）：`dirs` 收集全部目录相对路径（含空目录、父级在前）、`items` 收集文件（带 `relDir`）；`webkitGetAsEntry` 不可用时兜底退回 `dataTransfer.files`（文件夹丢失）。② 上传统一走 `runUploads(dirs, items)`：先 `POST /api/ensure-dirs` 批量建目录（已存在跳过、可重复执行），再逐文件上传（目标 = cwd/relDir，进度面板显示相对路径如 `myfolder/sub/file.txt`）；单文件大小预校验、停止上传与文件上传一致；上传中拖入会提示先完成/停止。③ 「上传文件夹」选择器入口移除（拖拽已覆盖），「操作」下拉只留一个「上传」（多选文件）。④ 后端：`EnsureRemoteDir(s, dir)` 递归补建缺失层级（逐段 `Sftp.Exists` + `CreateDirectory`，幂等跳过）+ `POST /api/ensure-dirs`（批量）；`/api/mkdir` 保持原语义
- 2026-08-16：双击打开文件提速 —— 根因：`/api/file-content` 用 JSON 返回，System.Text.Json 默认把所有非 ASCII（中文等）转义成 `\uXXXX`，大文本膨胀 3-6 倍且服务端转义 + 浏览器 JSON.parse 都极耗 CPU，且整文件读进内存后才返回（首字节迟迟不到；终端 cat 走已开 PTY 的 SSE 推流，首字节 ~1 RTT）。修复：① 改为**原始字节流返回**（`Results.Stream` + 传 fileLength 带 Content-Length），不再 JSON 包裹；② 二进制检查改为**开头 64KB NUL 嗅探**（真实二进制头部必有 NUL），通过后流式下发；③ 前端 `api.getFileContent` 改 fetch 流式读取（`onProgress({loaded,total,percent,chunk})` 回调），EditorModal 边收边显示 + 顶部进度条（chunk 攒在 pendingParts，~120ms 节流刷一次，结束后以完整内容为准）
- 2026-08-16：上传上限配置化 —— 单文件上传上限从仅环境变量改为「env 变量 → configs/env.json 的 maxUploadMb → 默认 1GB」三级，0/负数 = 不限制（Kestrel 不设 MaxRequestBodySize、/api/health 返回 maxUploadBytes=0，前端跳过预校验）；桌面壳启动时强制设 HXSFM_MAX_UPLOAD_MB=0 忽略大小限制
- 2026-08-16：上传进度修复 + 停止上传 —— ① 进度面板之前看不见的根因：`z-index:40` 低于列表 loading 遮罩的 `z-index:2000`，被盖住；改为 3000。② `api.uploadFile` 增加 `signal` 参数（AbortController → xhr.abort），面板加「停止上传」按钮，取消时提示「已取消上传」不报错。改 CSS 注意：Element Plus 遮罩 z-index 2000，自绘浮层要高于它
- 2026-08-16：上传进度 —— `api.uploadFile` 改用 XMLHttpRequest（fetch 无上传进度事件），新增 `onProgress(0-100)` 回调，401/错误语义与 fetch 版一致；FileManager 上传时列表上方浮出进度面板（`当前文件名 + i/n + el-progress 进度条 + 百分比`），多文件逐个上传逐帧更新
- 2026-08-16：文件列表删除/上传加 loading —— FileManager 新增 `deleting` 状态，删除（单个+批量）与上传期间 el-table 显示遮罩并附文字（`删除中…`/`上传中…`），此前只有上传下拉里一行“上传中…”文字且一关下拉就看不见；遮罩由 `tableLoading = loading || deleting || uploading` 驱动
- 2026-08-16：交互终端底部留白 —— `.xterm-wrap` 的 padding 从四边等距 8px 改为 `8px 8px 16px`（底部加宽），终端滚动到底时最后一行不再贴边。注意：fit() 按容器内容区高度算行数，改 padding 会自动把少算的行高让给底部留白，属预期
- 2026-08-16：应用图标全平台接入 + 产物零散文件 —— ① 两项目 `<ApplicationIcon>` 引用根目录 `logo.ico`（Windows exe 内嵌图标）；② 桌面壳窗口图标：`logo.png`（由 logo.ico 提取的 256x256 RGBA）作 `EmbeddedResource` 内嵌，启动时解到临时目录给 Photino `SetIconFile`（**必须用 PNG**：gdk-pixbuf 的 ICO 加载器对现代多尺寸 ico 支持差，会显示噪点）；③ **Linux 桌面图标**：build.sh 的 linux 发布把 `logo.png` 拷进发布目录，`hxsfm.desktop`/`install-desktop.sh` 的 `Icon=` 指向它（Linux 无 exe 内嵌图标机制，必须靠 .desktop 引用图标文件）；④ 构建/发布后清理 target 顺带删历史遗留的 logo.ico 散文件。产物目录最终：无 pdb（DebugType=embedded）、无 Data、无 logo 散文件
- 2026-08-16：Linux 桌面图标修复 —— 根因：`.desktop` 的 `Icon=` 规范只认**主题图标名或绝对路径**，相对路径（`Icon=logo.png`）解析不了，所以拷贝文件夹启动后没图标。修复：① 桌面壳首次启动时自装图标（`InstallLinuxIcon`：把 logo.png 拷进 `~/.local/share/icons/hicolor/{256..32}x*/apps/hxsfm.png` + 生成 `~/.local/share/applications/hxsfm.desktop`，`Icon=hxsfm` 主题名 + 绝对路径 Exec + `StartupWMClass`），文件夹拷到任意位置、启动一次后应用菜单/任务栏就有 logo；② build.sh 生成的 `install-desktop.sh` 同步改为主题安装（不再用绝对路径引用）；③ `hxsfm.desktop` 的 `Icon` 改主题名 `hxsfm`。注意：Linux 桌面图标不用 .ico（gdk-pixbuf 对多尺寸 ico 支持差），一律用 logo.png。**Windows 构建机产出的二进制/hxsfm.desktop 经常不带可执行位**（实测 -rw-r--r--），`install-desktop.sh` 会先 `chmod +x` 主程序与便携版启动器再安装（不要用 `[[ -x ]]` 做前置检查，否则报“未找到”）；网页端 favicon 已换 logo：`client/public/` 下 `favicon.ico`（多尺寸）、`favicon.png`（64x64）、`apple-touch-icon.png`（256，手机主屏），`index.html` 已引用，改图标后需 `npm run build` + `dotnet build` 同步到 bin
- 2026-08-16：build.sh 发布健壮性 —— 桌面发布前先 `rm -rf "$outdir"` 再 publish：`dotnet publish` 重复发布到同一目录（DeleteExistingFiles）可能把已存在但不在当前发布列表的文件清掉，中途失败还会留下缺原生库（Photino.Native.so/.dll）的不完整产物（与 mac-app 用 staging 目录同一原因）。另注意：build.sh 默认 PublishSingleFile=true，**dist 里没有散落的托管 DLL 是正常的**（全部打进单文件 bundle），必须有的是旁边的原生库 Photino.Native.* 和 WebView2Loader.dll（Windows）
- 2026-08-16：构建产物清理 —— ① 两项目 csproj 加 `<DebugType>embedded</DebugType>`，不再产出独立 .pdb（符号内嵌，产物目录干净且保留调试能力）；② 新增 `CleanOutputData` target（Build/Publish 后执行）删除输出目录里的 Data 内容，源 `Data` 目录也改为整体 `<None Remove="Data\**\*" />` 不随构建拷贝。**Data 是运行时在 ContentRoot 下按需生成的**（首启重建 secret.key/connections.json/settings.json），所以每次构建后输出干净、运行后又会出现是正常行为。**Debug 构建不再清理**（拆成两个 target：Build 钩子带 `Condition="'$(Configuration)' != 'Debug'"`，Publish 钩子无条件）——调试时直接跑 bin/Debug 里的 exe，Data 里的已保存连接/密钥保留，否则每次调试都要重新添加连接；Release 构建与所有 publish 仍清（发布产物绝不带 Data）。坑：不能靠 `$(PublishDir)` 判空区分 build/publish，普通 build 时它也有默认值 `bin\...\publish\`
- 2026-08-16：修复优雅停机被长连接拖死 —— 根因：`/api/logs/stream`、`/api/terminal/stream`、`/api/terminal/ws` 是"永不自行结束"的长连接（SSE 一直推、WS 一直等输入），Kestrel 停机时对这类进行中的请求只能干等 `HostOptions.ShutdownTimeout`（默认 30s）超时强杀，表现是"收到 SIGTERM/Ctrl+C 后进程迟迟不退"（docker stop 默认 10s 后还会直接 SIGKILL）。修复：① `Build()` 早期创建全局 `shutdownCts`，`ApplicationStopping` 时 `Cancel()` + 后台 `mgr.DisposeAll()` 关闭全部 SSH 会话；② 三个长连接端点把 `shutdownCts.Token` 链进各自取消源（ws 的 `linked` CTS、两个 SSE 的 `WithCancellation`/`WriteAsync`），停机信号一到循环立即结束。复现工程实测 `StopAsync` 从 **30s → 9ms**。**新增长连接端点时务必同样链入 `shutdownCts.Token`**
- 2026-08-15：登录「记住密码」—— 原来「记住我」只存 token，token 过期/登出后回登录页还要重输密码；现在勾选后把密码存 localStorage（`hxsfm_remember_pwd`），登录页 onMounted 自动回填；登录成功且勾选→存、取消勾选→删；**登出不删密码**（否则记住就没意义）。明文存，与 connections.json 明文凭据同一安全口径（仅本地/内网）
- 2026-08-15：去掉会话断开时的刷屏 toast —— SystemStatus.vue 的 10s 自动轮询失败（SSH 断开/空闲回收后 `Client not connected.`）会每 10 秒弹一次错；改为后台轮询 `load(true)` 静默失败（断开提示交给 App 的会话健康横幅），仅用户手动刷新/打开详情时才 toast
- 2026-08-15：终端宽度自适应 —— 之前 xterm 的 cols/rows 只在打开 pty 时算一次（容器宽/9、高/18 clamp），拉窗 / 拖分隔条 / 终端最大化后终端还是原宽度。修复：**前端** Terminal.vue 用 `ResizeObserver` 监听 `.xterm-wrap`，容器尺寸变化时 150ms 防抖 `xterm.resize(cols,rows)` 并通过 ws 发 `{type:'resize',cols,rows}`（ws 未开时只改显示）；**后端** `/api/terminal/ws` 的 resize 分支从“忽略”改为调用 SSH.NET `ShellStream.ChangeWindowSize(cols, rows, cols*8, rows*16)`（2026.0.0.1 版本确实支持动态改 pty 大小，原“不支持动态 resize”注释是错的，已删），让 shell 回绕列数跟随终端实际宽度。行列计算由 `@xterm/addon-fit` 的 `fit()` 按**真实单元格尺寸**计算（pty 开多大、显示多大完全同源；打开顺序：先建 xterm+fit → `terminalOpen(xterm.cols, xterm.rows)`）。`.xterm-screen` 不按 CSS 撑满是 xterm 设计行为——它的宽度是 JS 按 列数×单元格宽 写死的内联 style，字符网格不能拉伸，硬写 `width:100%` 没意义；想顶满容器只能靠 fit 选合适列数（最多余一条不足一个单元格的窄缝）
- 2026-08-14：前端整体切到 Element Plus（main.js 注册 EP+图标；组件全用 el-*）
- 2026-08-14：修复 5 处字段名 bug（connId→connectionId）、断开/重连、dev proxy 端口
- 2026-08-14：支持多服务器并发连接 —— App.vue 会话标签栏（自定义 pill tab，可关闭+确认）、新建连接对话框、每标签独立 FileManager/Terminal/EditorModal（v-show 保留状态）；后端本就支持多会话（ConcurrentDictionary），无需改动
- 2026-08-14：已保存连接增强 —— 顶栏「已保存连接」下拉（连接中也可一键再开一个，走 /api/connections/reconnect）；管理对话框；编辑对话框（`PUT /api/connections/{id}`，留空字段保持不变，凭据不返回）；连接表单/编辑表单支持别名（name 字段），标签与列表显示别名
- 2026-08-14：文件列表空白 bug —— 后端最小 API 序列化 FileEntry 为 camelCase，前端曾读 PascalCase，已统一改 camelCase
- 2026-08-14：终端 cwd 持久化 + 文件列表联动 —— SSH.NET 每次 CreateCommand 开新通道导致 cd 不保留，改为会话级 Cwd + 命令包装（pwd 兜底解析）；新增 `/api/cwd`；FileManager 加「跟随终端路径」checkbox（默认开）双向联动；自绘面包屑去掉 el-breadcrumb 的间隙/双斜杠问题；顺带修复连接后初始目录未用家目录（ConnectPanel 缺发 homeDirectory）
- 2026-08-14：终端回车丢焦点 —— el-input 在 busy 时被 `:disabled` 禁用，焦点被浏览器夺走；改为执行期间不禁用输入框 + `restoreFocus()`（ref.focus）兜底，真实测试机实测连续命令焦点保持
- 2026-08-14：交互终端 —— 后端 `ShellStream` + pty（`/api/terminal/open|stream|input|close`）+ 前端 xterm.js（`@xterm/xterm` 6.0.0，Terminal.vue 双模式 radio）。实测连真实测试机：ls、read 交互输入回显、nano 全屏打开并输入文字均正常；修掉 SSE 关闭时 `Results.Ok()` 重复设状态码异常；Ctrl 组合键无法用合成事件自动化测试（工具限制），真实键盘可用
- 2026-08-14：终端默认交互模式 + 路径双向同步 —— 默认选中「交互终端」（onMounted 自动打开）；后端注入 PROMPT_COMMAND 让 bash 提示符输出 OSC 7 携带 cwd，前端解析后文件列表跟随（终端 `cd /etc` → 面包屑变 `/etc`）；文件列表导航向终端注入 `cd`（点「上级」→ 终端提示符变 `/`）。checkbox（同步路径）关闭后两边完全独立：终端 cd 不动文件列表，文件列表导航也不注入终端 cd（实测：关闭时点 logs 目录终端提示符不变；打开后点上级终端收到 `cd /config`）。真实测试机实测通过
- 2026-08-14：工作区高度修复 —— `.workspace` grid 只有 columns 没有 rows，子项 `height:100%` 参照不到确定行高回退成内容高度，导致 FileManager/Terminal 卡片矮半截（.term 曾只有 119px、xterm 显示区 37px）。修复：`grid-template-rows: minmax(0,1fr)`（窄屏 media query 改 `1fr 1fr` 两行均分）；xterm 补 `.xterm-viewport height:100%`；`openInteractive` 加 `waitForSize()` 等容器有实际尺寸再算 pty 行列数。双列实测 .fm/.term 均 256px 撑满。
- 2026-08-14：会话本地化持久化 —— 打开中的 SSH 会话 + 各自 cwd 存 localStorage（`hx_workspace_v1`，防抖 400ms 写入），刷新/下次打开自动恢复：`restoreWorkspace` onMounted 逐个 `reconnect(profileId)` + 恢复路径。后端 connect/reconnect 响应新增 `profileId`（Upsert 返回最终 profile，去重后保留原 Id），前端 SavedConnections/App 透传。两个坑：① FileManager externalPath watch 必须 `immediate: true`（挂载时已有恢复路径才触发）；② 交互终端 shell 启动时 OSC 7 会先推初始目录覆盖恢复路径——Terminal 注入恢复 cd 期间 `oscIgnored` 忽略 OSC 7（500ms），恢复完成后才推给 App。断开连接不清 localStorage（下次仍自动恢复）；删除 saved connection 后 profileId 失效自动跳过
- 2026-08-14：工作区布局改造 —— 终端在**左**、文件列表在**右**（workspace 改 flex，终端 `flex: 0 0 var(--term-w, 58%)`，文件 `flex: 1`）；中间 `.ws-divider` 可拖拽调宽（mousedown/mousemove，30%-75% clamp，`--term-w` CSS 变量）；终端头部最大化按钮（`termMax` → workspace.term-max，文件列表/分隔条隐藏，终端占满，再次点击还原）；窄屏单列终端在上文件在下，**分隔条横放可拖拽调高度**（`startResize` 按 `getComputedStyle(el).flexDirection` 自动判断拖宽度还是高度，`--term-h` 变量，30%-75% clamp；双列拖 `--term-w`）；文件列表修改时间格式去掉秒；实时操作日志默认隐藏（`logEnabled` 默认 false，顶栏按钮可开关）；文件列表去掉操作列，改为**行右键菜单**（el-table `@row-contextmenu` → 自定义 fixed 定位菜单：编辑（isText）/下载/重命名/删除，点击外部关闭，位置 clamp 防溢出），「编辑」项不依赖 isText：非目录均显示（后端 /api/file-content 自己拒绝目录/超大/NUL 二进制，前端只管放行）（`YYYY/MM/DD HH:mm`）。**xterm 6 高度链**：`.xterm`（height:100%）→ `.xterm-viewport`（absolute+100%）→ `.xterm-scrollable-element`（**必须 height:100%**，xterm.css 未给它高度，否则保持内容高度溢出、滚动失效）→ `.xterm-screen`（100%）；`.xterm-rows`/screen 内容行由 xterm 内部 buffer 管理，scrollback 滚动靠 xterm setScrollPosition 重渲染，不依赖 DOM overflow
- 2026-08-14：Desktop 白屏真正修复 —— 根因有两个：① Photino.NET 3.0.14 在 Windows 上窗口失效（实测只剩标题栏），升到 4.0.16；② WebView2 只能在 STA 线程创建，.NET 主线程默认 MTA → CreateCoreWebView2Environment 报 0x80010106（RPC_E_CHANGED_MODE）被 Photino 静默吞掉，窗口全白且无任何 webview2 子进程。修复：窗口创建挪到 `Thread.SetApartmentState(STA)` 的专用线程（仅 Windows 设 STA），入口保持同步；关窗后 `StopAsync` 停 Kestrel。用最小 WinForms+WebView2 和 Photino 复现工程对照实验定位（STA 成功渲染、MTA 复现白屏），前两个"修复"提交（同步入口、拷 wwwroot）本身正确但都不是真正原因。实测窗口正常渲染完整 UI（Kestrel 日志可见 WebView2 的 GET / + js/css 200）
- 2026-08-14：文件列表工具栏收拢 + 行多选 —— 「上传/新建目录」收进「操作」下拉（el-dropdown：新建目录/上传/删除三项，上传触发隐藏 file input），工具栏只剩 同步路径/上级/刷新/操作；**删除项为批量删除**（`selectedItems` computed 过滤当前列表选中项，未选中时 disabled 并显示数量「删除（N）」；确认后逐个 `api.remove`（dir 用 parentDir(fullPath)），完成后清空选中并 reload）；行选择改为 Windows/Mac 风格多选：隐藏 selection 列 checkbox（CSS display:none，列宽 1px），`@row-click` 自处理——普通单击单选（clearSelection+toggleRowSelection）、Ctrl/Cmd 单击加减选（toggle 不重置其他）、Shift 单击范围选（lastSelected 到当前行 items 全选中，row-key=fullPath），`rowClassName` 高亮 `.row-selected`（背景 #e3efff）；双击目录/文件行为保留（打开/编辑）。`.fm` 卡片整体 `user-select:none` 屏蔽浏览器原生文本选中（行选择/拖拽不出高亮，实测 getSelection 为空）。实测：单选、Ctrl 加选、Meta 减选、Shift 范围选（logs→test 中间 4 项）、批量删除（btest 内 4 项 + 3 个 txt）全部通过
